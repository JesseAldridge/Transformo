import time, osfrom os.path import join, expanduser, existsfrom jca.web_stuff.web_get import downloaddef download_page(s):  dir_path = join(expanduser('~'), 'Desktop', 'downloads')  if not exists(dir_path):  os.mkdir(dir_path)  local_path = join(dir_path, s.replace('/', '-').replace(':', '-'))  download(s, local_path)  with open(local_path) as f:    return f.read()if __name__ == '__main__':  print download_page('http://www.google.com/')